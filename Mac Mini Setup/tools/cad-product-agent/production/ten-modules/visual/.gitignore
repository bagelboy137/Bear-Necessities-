# Transient render-driver state, regenerated every run.
render-job.json
render-job-result.json
cycle-overrides.json
.render.lock

# Scene-sweep scratch: throwaway experiment renders. The recipes that produced
# them are kept in scene-variants*.json, so these are reproducible, not evidence.
renders/*-v[0-9]-*.png
renders/*-s[0-9].png
renders/*-g[0-9].png
renders/*-z[0-9].png
__pycache__/
render-progress.json
render-telemetry.json
render-trace.log
.probe.py
.fusion_render.prepared.py
